# Connect to MongoDB running locally on port 27017 and use (or create) the "mental_health_db" database.
spring.data.mongodb.uri=mongodb://localhost:27017/mental_health_db

# Run the server on port 8080.
server.port=8080
