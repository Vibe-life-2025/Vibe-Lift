# Database Configuration: Update with your MySQL credentials and database name
spring.datasource.url=jdbc:mysql://localhost:3306/mental_health_db
spring.datasource.username=root
spring.datasource.password=yourpassword

# Hibernate settings: Automatically update the database schema and log SQL statements
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server configuration (optional)
server.port=8080
